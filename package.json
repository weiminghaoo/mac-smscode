{
  "name": "mac-smscode",
  "version": "1.0.4",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "rimraf dist && cpy 'src/**/*.json' 'src/**/*.png' dist && ncc build ./src/index.js -w -o dist",
    "build": "rimraf dist && cpy 'src/**/*.json' 'src/**/*.png' dist && ncc build ./src/index.js -o dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "core-js": "^3.6.5",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "@vercel/ncc": "^0.36.1",
    "copy": "^0.3.2",
    "copyfiles": "^2.4.1",
    "cpy-cli": "^4.2.0",
    "rimraf": "^5.0.1",
    "utools-api-types": "^3.0.0"
  }
}
